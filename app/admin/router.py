"""Admin HTTP routes.

Placeholder — no admin endpoints are exposed yet. The `/api/bookings/database`
endpoint in app/booking/router.py is a temporary admin-shaped view that any
authenticated user can hit; it will move here once a real admin role exists.
"""

from fastapi import APIRouter

router = APIRouter()
