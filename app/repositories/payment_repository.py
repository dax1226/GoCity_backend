"""Payment persistence layer.

Stub. There are no payment / wallet / transaction tables yet (see
app/models/payment.py). Once they exist, queries for wallet balance
lookup, transaction insert, and refund updates live here.
"""

from sqlalchemy.orm import Session  # noqa: F401
