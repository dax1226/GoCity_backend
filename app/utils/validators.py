"""Reusable validators (phone numbers, vehicle plate format, etc.).

Placeholder. Pydantic field validators that are shared across multiple
schemas belong here so each schema file can `from app.utils.validators
import ...` instead of redefining the rule.
"""
