"""Notification-domain service helpers.

Stub. The demo-message templates and simulator helpers currently live
inline in app/notification/router.py. They will move here, and the real
driver-side notification creation flow will join them, when the driver
panel ships.

See also app/services/notifications.py for cross-cutting helpers
(push delivery, fan-out) that don't belong to a single slice.
"""
