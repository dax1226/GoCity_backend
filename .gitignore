# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
env/

# ── Secrets & environment variables ─────────────────────────────────────────
# NEVER commit these. Add real keys to your platform's secret store (AWS,
# Railway, Render, etc.) and keep .env.example for documentation only.
.env
.env.*
!.env.example

# Never commit Firebase service account credentials
*firebase-adminsdk*.json

# Local SQLite databases
*.db
*.sqlite
*.sqlite3
gocity.db

# Node (if used in this folder)
node_modules/
package-lock.json

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
