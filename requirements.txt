fastapi
uvicorn[standard]
pydantic
pydantic-settings
python-dotenv
firebase-admin
sqlalchemy
psycopg2-binary
geoalchemy2
email-validator
alembic
# Redis — geospatial hot path for live driver locations (app/geo).
redis>=5.0
bcrypt
python-jose[cryptography]
# Optional — only needed when going live with Razorpay (see app/payment/routes.py).
# The payment endpoints fall back to stub responses if this is not installed.
razorpay
httpx

# Driver document uploads (multipart form / file uploads).

cloudinary

python-multipart
